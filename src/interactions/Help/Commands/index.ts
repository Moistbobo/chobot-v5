import { INTERACTION_TYPE } from 'config/interactionConfig';
import interaction from './interaction';

const InteractionCommands = {
  interaction,
  id: INTERACTION_TYPE.HELP_COMMANDS,
};

export default InteractionCommands;
